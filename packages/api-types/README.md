# api-types

TypeScript types generated from the platform's OpenAPI spec (`tools/openapi/`). Consumed
by `apps/web` and `apps/mobile` so both clients share one source of truth for the `/v1`
contract. Never hand-edited — regenerate from the spec.
