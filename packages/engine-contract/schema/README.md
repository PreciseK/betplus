# engine-contract/schema

JSON Schema definitions for the Engine Contract request and response shapes
(`resolve`, `replay`, `describe`), shared by every game engine and the platform's
`Games/EngineClient.php`. Populated by the story that defines the Engine Contract.
